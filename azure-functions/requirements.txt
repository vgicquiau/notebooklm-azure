# Requirements pour les Azure Functions ADG-M (Python 3.11, runtime v4)
# Installer dans le venv : pip install -r requirements.txt

# Azure Functions runtime
azure-functions==1.19.0

# Blob Storage (pour Blob Trigger + upload rétro-docs)
azure-storage-blob==12.19.0

# Azure OpenAI
openai==1.40.0

# Pinned transitively: openai==1.40.0's internal HTTP client wrapper still passes
# a `proxies=` kwarg to httpx.Client.__init__(), which httpx>=0.28 removed entirely
# (TypeError: Client.__init__() got an unexpected keyword argument 'proxies').
# Without this pin, pip/Oryx resolves the latest httpx and every AzureOpenAI(...)
# construction crashes instantly. Keep <0.28 until openai is upgraded past this.
httpx==0.27.2

# Managed Identity auth (oai-nlmazure-prod a disableLocalAuth=true -- pas de cle API,
# auth obligatoire via DefaultAzureCredential + role RBAC "Cognitive Services OpenAI User")
azure-identity==1.17.1

# Neo4j Python driver
neo4j==5.22.0

# SQL Server (Azure SQL via ODBC)
pyodbc==5.1.0

# Validation de schéma
pydantic==2.7.0

# Utilitaires
python-dotenv==1.0.1
