fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.20
azure-search-documents==12.0.0
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0
openai==1.57.0
python-dotenv==1.0.1
pydantic==2.10.3
tenacity==9.0.0
azure-monitor-opentelemetry==1.8.8

# Proxy serveur-à-serveur vers fn-adgm-graph (api/routers/graph.py) — déjà présent
# en transitif via openai, épinglé ici en direct car utilisé explicitement.
httpx==0.28.1

# Lecture directe de l'instance Neo4j neo4j-legacykb (api/services/legacykb_client.py)
# via l'API HTTP transactionnelle (pas le driver Bolt -- cf. docstring du module,
# Zscaler casse le handshake TLS sur le port Bolt non-HTTP). Déjà présent en transitif
# via azure-identity/openai, épinglé ici en direct car utilisé explicitement.
requests==2.34.2
